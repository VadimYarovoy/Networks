#import "@preview/tablex:0.0.5": *

= Результаты измерений
== Измерение мощности сигнала

#align(center)[
    #tablex(
    columns: 4,
    align: center + horizon,
    auto-vlines: false,
    header-rows: 2,

    /* --- header --- */
    colspanx(4)[*Мощность*],
    [], [*Место 1*], [*Место 2*], [*Место 3*],
    /* -------------- */

    [*2G*]  , [], [], [],
    [*3G*]  , [], [], [],
    [*4G*]  , [], [], [],

    [], [*Место 1*], [*Место 2*], [*Место 3*],
    [*WiFi*], [], [], [],

    )
]

Для контроля включения соответствующего режима сети, была проверена установка нужного режима и отсутствие переключения режима работы под управлением базовой станции сети (должно было соответствовать 100%).

#align(center)[
    #tablex(
    columns: 3,
    align: center + horizon,
    auto-vlines: false,
    header-rows: 1,

    // color the last column's cells
    // based on the written numberl

    /* --- header --- */
    colspanx(3)[*Статистика*],
    /* -------------- */

    [], [], [],
    [], [], [],
    [], [], [],
    [], [], [],

    )
]

== Расстояния до базовых станций

#align(center)[
    #tablex(
    columns: 4,
    align: center + horizon,
    auto-vlines: false,
    header-rows: 1,

    // color the last column's cells
    // based on the written numberl

    /* --- header --- */
    [], [*2G*], [*3G*], [*4G*],
    /* -------------- */

    [*Место 1*], [], [], [],
    [*Место 2*], [], [], [],
    [*Место 3*], [], [], [],
    )
]

Примечание: информация о базовых станциях взята из приложения Opensignal, так как в приложении Network Cell Info Lite информация о местоположении станций некорректная.

== Измерения скорости
#align(center)[
    #tablex(
    columns: 6,
    align: center + horizon,
    auto-vlines: false,
    header-rows: 1,

    /* --- header --- */
    [], [*1*], [*2*], [*3*], [*4*], [*5*],
    /* -------------- */
    [*2G*], [], [], [], [], [],
    [*3G*], [], [], [], [], [],
    [*4G*], [], [], [], [], [],
    )
]

== Расчет ожидаемой мощности сигнала по применимым моделям
$ P_r = P_t - P L (d) "дБм" $ 

=== Вычисление затухания
- *2G*
- *3G*
- *4G*

=== Полученные мощности и сравнение с измеренными значениями

#align(center)[
    #tablex(
    columns: 7,
    align: center + horizon,
    auto-vlines: false,
    header-rows: 2,

    /* --- header --- */
    [], colspanx(2)[*Мощность*], colspanx(2)[*Мощность*], colspanx(2)[*Мощность*],
    [], [*Эксперимент*], [*Расчет*], [*Эксперимент*], [*Расчет*], [*Эксперимент*], [*Расчет*],
    /* -------------- */
    [*2G*]  , [], [], [], [], [], [],
    [*3G*]  , [], [], [], [], [], [],
    [*4G*]  , [], [], [], [], [], [],
    [*WiFi*], [], [], [], [], [], [],

    )
]
